public enum AstalNotifd.Urgency {
    LOW = 0,
    NORMAL = 1,
    CRITICAL = 2,
}

public enum AstalNotifd.ClosedReason {
    EXPIRED = 1,
    DISMISSED_BY_USER = 2,
    CLOSED = 3,
    UNDEFINED = 4,
}

public enum AstalNotifd.State {
    DRAFT,
    SENT,
    RECEIVED
}
